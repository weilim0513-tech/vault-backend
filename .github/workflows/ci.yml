name: Vault CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # JOB 1: 빌드 및 테스트 (CI)
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. 소스코드 내려받기 (v4가 최신 안정 버전)
      - name: Checkout Source Code
        uses: actions/checkout@v4

      # 2. JDK 21 세팅 (GraalVM)
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'graalvm'
          cache: 'gradle'

      # 3. application-prod.yml 껍데기 생성
      - name: Create dummy application-prod.yml
        run: |
          mkdir -p src/main/resources
          echo "spring.config.activate.on-profile: prod" > src/main/resources/application-prod.yml

      # 4. Gradle 권한 부여
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 5. 빌드 및 테스트 실행 (🔥 중요: -x test 제거함!)
      # 이제 Testcontainers가 돌면서 테스트를 통과해야만 배포로 넘어갑니다.
      - name: Build with Gradle
        run: ./gradlew clean build

      # 6. 빌드 결과물(Jar) 저장
      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: vault-jar
          path: build/libs/*.jar
          retention-days: 1

  # JOB 2: 도커 이미지 빌드 및 배포 (CD)
  deploy:
    needs: build
    if: github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      # 1. Jar 파일 다운로드
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: vault-jar
          path: build/libs/

      # 2. 도커 허브 로그인
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3. 도커 이미지 빌드 & 푸시
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/vault-server:latest

      # 4. AWS EC2 접속 및 배포 실행
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            echo "🚀 배포 시작..."
            
            # 1) 실행 중인 컨테이너 정리
            if [ $(docker ps -q -f name=vault-server) ]; then
              docker stop vault-server
            fi
            if [ $(docker ps -aq -f name=vault-server) ]; then
              docker rm vault-server
            fi
            
            # 2) 최신 이미지 다운로드
            docker pull ${{ secrets.DOCKER_USERNAME }}/vault-server:latest
            
            # 3) 컨테이너 실행 (Secrets 적용)
            # GitHub Secrets 값을 주입
            docker run -d \
              --name vault-server \
              --network host \
              -e TZ=Asia/Seoul \
              -e DB_USERNAME=${{ secrets.PROD_DB_USERNAME }} \
              -e DB_PASSWORD=${{ secrets.PROD_DB_PASSWORD }} \
              -e JWT_SECRET=${{ secrets.PROD_JWT_SECRET }} \
              ${{ secrets.DOCKER_USERNAME }}/vault-server:latest
            
            # 4) 미사용 이미지 정리
            echo "🧹 사용하지 않는 도커 이미지 정리..."
            docker image prune -f
            
            echo "✅ 배포 완료!"